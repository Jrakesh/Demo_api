<div class="container-fluid">
  <p id="notice"><%= notice %></p>
  <h4>Album: <strong><%= @album.name %></strong></h4>      
  <h4>Name: <strong><%= @picture.name %></strong></h4> 
  <h4>Description: <strong><%= @picture.description %></strong></h4>
  <br>
  <%= link_to 'Edit', edit_album_picture_path(@picture.album, @picture), class: "save btn btn-warning" %>
  &nbsp;&nbsp;
  <%= link_to 'Back', album_path(@album), class: "save btn btn-info" %>
  <div class="row">
    <div class="col-lg-12">
    <h1 class="page-header">Album pictures<small>images</small></h1>
    </div>
  </div>
  <!-- /.row -->

  <div class="row">
      <div class="col-md-3 portfolio-item">
        <%#= image_tag(pic.image.url, class: "img-responsive")%>
        <%#= image_tag(@picture.image.url, width: "400px")%>
      </div>
  </div>
    
</div>